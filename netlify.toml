[build]
  base = "."
  publish = "public/"
  command = "hugo --gc --minify --buildFuture"

[context.production.environment]
  HUGO_VERSION = "0.128.1"
  HUGO_ENV = "production"
  TZ = "Europe/Paris"
  